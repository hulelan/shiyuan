# 补齐诗词正典 —— 交接说明

写给接手这件事的人。前面已经查清的、已经备好的、以及几处会绊人的地方，都在这里。
**导入这一步的代码尚未写**：名单和事实齐了，脚本没有。

## 一、现在缺什么

本站 4,300 篇。上游（chinese-poetry，公有领域）唐诗 57,607、全宋诗 254,248、
宋词 21,050，合计 332,905 —— 本站占 **1.3%**，且不是挑出来的 1.3%。

导入当初取的是 `poet.tang.0.json`（《全唐诗》开卷）等几个文件的前若干条，于是：

| 朝代 | 本站 | 实情 |
|---|---|---|
| 先秦 | 1,198 | 诗经 + 论语孟子，尚可 |
| 汉 | **0** | 古诗十九首、汉乐府，全无 |
| 魏晋 | **1** | 只有陶渊明《饮酒·其五》 |
| 南北朝 | **0** | 木兰诗、敕勒歌，全无 |
| 隋 | **0** | |
| 唐 | 2,142 | 其中 **67% 是郊庙歌辞与乐府** —— 开卷即宫廷祭祀乐章。太宗皇帝 101 篇，白居易 18 篇 |
| 五代 | **0** | 李煜挂在唐下 |
| 宋 | 502 | 柳永 212 篇独大；**苏轼 1 篇，辛弃疾、陆游、欧阳修、王安石全无** |
| 元 | 200 | 关汉卿 170 |
| 明 | **0** | |
| 清 | 257 | 全部是纳兰性德 |

找不到的名篇：离骚、古诗十九首、洛神赋、归园田居、敕勒歌、木兰诗、
茅屋为秋风所破歌、**长恨歌**、**琵琶行**、陋室铭、岳阳楼记、醉翁亭记、
**念奴娇·赤壁怀古**、前赤壁赋、声声慢、过零丁洋、石灰吟、己亥杂诗。

## 二、已经备好的东西

- **`data/curated/canon_poets.json`** —— 该补谁。80 位、目标 2,331 篇，按"读者会
  去找谁"排，不是按上游篇数排：按篇数排只会补进大量无人读的卷帙（上游篇数第一
  的唐人是白居易 3,009，第三是易静的 721 首占卜诗）。
- **`data/curated/canon_roster.tsv`** —— 5,139 位作者，上游篇数 + 本站已有篇数。
- **`data/curated/author_years.json`** —— 99 位唐人生卒，用于按作者估年（已在用）。

## 三、一个关键发现：上游自带热度排名

`rank/poet/poet.tang.rank.*.json`、`rank/poet/poet.song.rank.*.json`、
`rank/ci/ci.song.rank.*.json` —— 336 个文件、332,909 条，与诗文一一对应。每条形如：

```json
{"author":"太宗皇帝","title":"帝京篇十首 一","baidu":20600,"google":50400,"bing":157000,"so360":1120,"bing_en":97600}
```

**这就是挑篇目的依据。** 同一位诗人取哪 60 首，不必凭印象，也不必再跑模型：
按搜索热度取前 N。join 用 `(author, title)`；上游是繁体，须 `opencc t2s` 转换后再对。

## 四、动手的顺序

1. 写 `tools/import_canon.py`：
   - 读 `canon_poets.json` 与三套上游文件（唐诗 / 全宋诗 / 宋词）；
   - 用 rank 排序，每位取 cap 篇，扣掉本站已有的；
   - 繁转简、生成拼音（`pypinyin`，用仓库的 `.venv`）；
   - 按 `tools/corpus_lib.py` 的 SOURCE_FIELDS 写成 jsonl，并入 `data/source/`；
   - 以 `(author, title, text)` 去重 —— 上游同一首会在不同卷里重出。
2. 跑 `tools/build_site_data.py` 重建 `data/site/`。
3. 只动了年份相关的东西，跑 `tools/rebuild_timeline.py` 即可。
4. 新篇目没有译文/注释/赏析，要 `tools/enrich_glm.py` 才有 —— 那是另一笔开销，
   可以分批，也可以先让它们只有原文。

## 五、几处会绊人的地方

- **`MAX_CHARS = 360` 把长诗滤掉了**（`tools/build_corpus.py:48`）。《长恨歌》
  《琵琶行》《茅屋为秋风所破歌》不在库里就是因为这一条，不是因为没导。
- **`build_site_data.py` 会先清空 `data/site/`** 再重建（KEEP 之外的都删）。跑之前
  确认 `data/enrich/` 是全的 —— 译文都在那里，site 只是产物。
- **时间轴的年份有三种底子**：逐篇断代 / 按作者估算 / 只知朝代。占位年份一律不进
  直方图，否则整个朝代堆在同一格。改 `build_site_data.py` 里那段，记得同步改
  `tools/rebuild_timeline.py`。
- **不要去爬古诗文网 / 古文网的译文、注释、赏析** —— 那是有版权的编辑成果。原文
  是公有领域，chinese-poetry 也是自由许可的，导入正文没有问题。
- 上游 `全唐诗/` 目录里同时装着 `poet.song.*`（全宋诗），别被目录名骗了。

## 六、缓存在哪

已下载的上游文件（唐诗 25MB、全宋诗 141MB、宋词 16MB、rank 336 个文件）在本次
会话的临时目录里，会被清掉。重下即可 —— `tools/build_corpus.py` 的 `RAW` 常量
就是地址，`fetch()` 自带缓存。
